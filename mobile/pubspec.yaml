name: abel_logistics
description: Abel Logistics Agent App

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ">=1.1.0 <2.0.0"
  provider: ">=6.1.1 <7.0.0"
  image_picker: ">=1.0.4 <2.0.0"
  file_picker: ">=8.0.0 <9.0.0"
  shared_preferences: ">=2.2.2 <3.0.0"
  intl: ">=0.18.1 <1.0.0"
  path_provider: ">=2.1.0 <3.0.0"
  open_file: ">=3.3.2 <4.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ">=3.0.0 <4.0.0"

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
